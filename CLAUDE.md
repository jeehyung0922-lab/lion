# lion (시차 프론트엔드)

교대근무자 생활 리듬 서비스 "시차"의 프론트엔드.
React 19 + TypeScript + Vite 8, Tailwind v4, PWA. 백엔드는 별도 레포(`shiftrhythm-backend`).

**이 레포는 parkilha 단독 작업이다(2026-08-19 확정).** 다른 사람과 계약을 맞출 일이 없으므로,
애매하면 물어보지 말고 이 문서와 design.md 기준으로 판단해서 진행한다.
다만 백엔드 API 계약은 여전히 남의 것이다 — 프론트에서 바꿀 수 없다.

## 먼저 읽을 것

- [README.md](README.md) — 스택, 폴더 구조, 라우팅/온보딩 분기, 모드 7종
- [design.md](design.md) — 디자인 토큰과 컴포넌트 패턴. **토큰 값을 바꾸면 이 문서도 같이 갱신한다.**
- 타입 정본은 백엔드의 `/v3/api-docs`. `src/types/index.ts`는 그걸 손으로 옮긴 사본이라,
  화면이 이상하면 코드보다 계약이 바뀐 건 아닌지 먼저 확인한다.

## 컨셉 언어 규칙 (UI 문구 작성 시 준수)

- **"실패"라는 단어 금지.** 계획이 깨져도 "다시 조정"이다.
- **여행 이미지(비행기·캐리어·여권) 배제.** "시차"는 은유지 여행이 아니다.
- 시차 언어(어느 시간대에 사는지, 몇 시간을 건너는지)는 홈 상단·주간 예보·콜렉트북에서만.
  오늘의 루틴과 재설계 화면은 **정확한 시각 우선** — 시차 언어를 쓰지 않는다.

## 명령

```bash
npm run dev            # 개발 서버
npm run build          # tsc -b && vite build — 커밋 전 반드시 통과시킬 것
npm run lint           # oxlint
npm run format:check   # prettier
```

## 함정

- **CI가 없다.** 위 build/lint/format:check를 푸시 전에 직접 돌린다. 아무도 대신 봐주지 않는다.
- 브랜드 색은 반드시 `--color-brand`를 쓴다. shadcn의 `--color-accent`와 헷갈리면
  GNB 활성색이 깨진다(전례 있음). 자세한 토큰은 design.md.
- 리포트 API(주간·월간·일간)는 `skipUserId: true`로 `X-User-Id`를 **일부러 붙이지 않는다**
  — 부스 시연용 고정 계정이다. 일관성 맞춘다고 지우지 말 것.
- `VITE_API_BASE_URL`은 백엔드 cloudflared 터널 주소라 터널이 재시작되면 바뀐다.
  화면이 전부 비거나 모든 요청이 실패하면 코드보다 여기를 먼저 의심한다.
- **다크 테마만 있다.** 라이트 모드 대응 코드를 새로 추가하지 않는다.
- 근무표 사진 파싱 결과는 AI 초안이라 매번 미세하게 다르다.
  반드시 사용자가 확인·수정하는 화면을 거쳐 확정한다 — 바로 저장하지 않는다.

## 작업 규칙

- 기능마다 브랜치를 파서 PR로 main에 머지한다. main 직접 커밋 금지.
  단독 작업이지만 유지한다 — 리뷰가 아니라 **되돌릴 지점**을 남기는 게 목적이다.
- main push는 Vercel 자동 배포다. 행사 종료까지 URL이 살아 있어야 하므로,
  빌드가 깨진 채로 main에 올리지 않는다.
- 페이지 컴포넌트가 500줄을 넘으면 쪼갠다(현재 `ReportPage` 534줄, `MainPage` 440줄).
- `.env`는 읽지도, 수정하지도, 커밋하지도 않는다. 값이 필요하면 `.env.example`을 본다.
